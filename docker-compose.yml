services:
  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
    ports:
      - "5173:80"
    extra_hosts:
      # Needed on Linux Docker; Docker Desktop (Win/Mac) provides this automatically
      - "host.docker.internal:host-gateway"
